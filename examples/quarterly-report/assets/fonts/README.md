# Fonts

A real ADF container embeds subsetted font files here (e.g.
`SourceSerif-Regular.otf`, `SourceSans-Bold.otf`) whose metrics match the
`metrics_hash` pins in `manifest.json`. They are omitted from this example to
keep the repository light; the manifest lists them as placeholders so the
structure is clear.
